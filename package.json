{
  "name": "ctalmap",
  "version": "0.2.2",
  "description": "Lights an led where trains are in real time",
  "main": "index.html",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html",
    "cors": "node cors-anywhere/server.js",
    "deploy": "npm run build && firebase deploy --only hosting:ctalmap"
  },
  "author": "hewham",
  "license": "ISC",
  "dependencies": {
    "handlebars": "^4.7.7",
    "live-server": "^1.2.1",
    "parcel-bundler": "^1.12.5"
  }
}
